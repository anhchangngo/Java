The expression to compute the discriminant of a cubic polynomial with coefficients b, c, and d is:

discriminant = b^2 * c^2 - 4 * c^3 - 4 * b^3 * d - 27 * d^2 + 18 * b * c * d

 the terms correspond to specific components of the cubic polynomial and are combined to calculate the discriminant.
